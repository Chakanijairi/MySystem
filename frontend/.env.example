# Production (Vercel): your Render API URL, no trailing slash.
# Set this in Vercel → Project → Settings → Environment Variables (must be present at build time).
# VITE_API_URL=https://your-service.onrender.com

# Leave unset for local dev (Vite proxy sends /api to localhost:5000).
